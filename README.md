# @kittipos/openclaw-notebook-tools

OpenClaw plugin: read and edit Jupyter notebooks (`.ipynb`).

See `Notebook-Tools-Spec.md` for the full specification and `CLAUDE.md` for
implementation guidance.

## Quick start

```bash
pnpm install
pnpm build
openclaw plugins install $(pwd)
openclaw gateway restart
```
